var words1 = ["Farm", "Cheese", "Apple", "Character", "Planet", "Godfrey", "Orange", "Story", "Schelude", "Often",
 "Ocassionally", "Always", "Never", "Cow", "Animal", "Dog", "Sheep", "Banana", "Rarely", "However", "Oil", "Defined", "Variable"];
const input = document.getElementById("inputbox");
const isValid = input.value.words1
input.className = isValid ? "valid" : "invalid";

input.addEventListener("input",() => {
	if (isvalid)
	{
		input="valid" 
	}
	else
	{
		input="invalid"
	}	

input.addEventListener("sumbit",() => {
	if (isvalid)
	{
		input="valid" 
	}
	else
	{
		input="invalid"
	}	